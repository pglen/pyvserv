INSTALL

# Preliminary

1.) Create a pyvserv environmant

sudo apt-get update -y
sudo apt-get upgrade -y

sudo useradd -m -s /bin/bash pyvserv
sudo passwd pyvserv
sudo usermod -aG sudo pyvserv

su - pyvserv


On Ubuntu:

    pip install pycrypto
    pip install bcrypt

    (pip3 if newer Linux)

  ... or ...

    apt install python3-psutil
    apt install python3-crypto

On msys the following dependencies where needed:

    pacman -S mingw-w64-i686-gcc

    pip install passlib
    pip install pycrypto
    pip install bcrypt

#Switched to pycrypto, no libgcrypt is needed
#pacman -S mingw-w64-i686-libgcrypt

alternate:

    sudo apt install python-bcrypt
    sudo apt install pycrypto

On Fedora:

    sudo yum install python-bcrypt
    sudo yum install pycrypto

# EOF








